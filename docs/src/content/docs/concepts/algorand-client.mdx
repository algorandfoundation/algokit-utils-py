---
title: "Algorand Client"
description: "AlgorandClient is the entry point to AlgoKit Utils: a single object that connects to a network, manages signers, and exposes the interfaces for building and sending transactions."
---

import { CardGrid, LinkCard } from "@astrojs/starlight/components";
import RemoteCode from "/src/components/RemoteCode.astro";

`AlgorandClient` is the entry point to AlgoKit Utils. A single instance connects to a network and exposes the rest of the library: account management, transaction construction and sending, and asset and application operations. It also manages concerns you would otherwise implement yourself, such as caching suggested network parameters and resolving each sender to its registered signer.

Create one client at startup and share it across your application; every capability described below is reachable from that instance.

## Create a Client

Static factory methods cover the common ways to connect:

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-py/refs/heads/main/examples/concepts/algorand_client.py"
  snippet="INSTANTIATE_ALGORAND_CLIENT"
  lang="py"
  frame="none"
/>

| Factory method                       | Connects to                                                                      |
| ------------------------------------ | -------------------------------------------------------------------------------- |
| `default_localnet()`                 | LocalNet on the default ports                                                    |
| `testnet()` / `mainnet()`            | TestNet or MainNet via the free [Nodely](https://nodely.io/docs/free/start) tier |
| `from_environment()`                 | The network described by environment variables                                   |
| `from_config(algod_config=..., ...)` | Custom server URLs, ports, or tokens                                             |
| `from_clients(algod=..., ...)`       | Existing SDK client instances                                                    |

For code that runs in more than one environment, use `from_environment()`. It reads `ALGOD_SERVER` (plus `ALGOD_PORT`, `ALGOD_TOKEN`, and `INDEXER_SERVER` / `KMD_PORT` when present) and falls back to the default LocalNet configuration when `ALGOD_SERVER` isn't set, so the same code runs against LocalNet on your machine and against TestNet or MainNet in deployment.

## Navigate the Client

Each area of functionality is exposed as a property or method on the client:

| Property or method                      | Purpose                                                  |
| --------------------------------------- | -------------------------------------------------------- |
| `algorand.account`                      | Account creation, funding, lookup, and signer management |
| `algorand.send`                         | Sending single transactions                              |
| `algorand.create_transaction`           | Building unsigned transactions without sending them      |
| `algorand.new_group()`                  | Building and sending atomic transaction groups           |
| `algorand.asset`                        | Asset lookups and opt-ins/opt-outs                       |
| `algorand.app`                          | App state reads and smart contract compilation           |
| `algorand.app_deployer`                 | Idempotent app deployment                                |
| `algorand.client`                       | App clients, SDK clients, and network details            |
| `algorand.register_error_transformer()` | Transforming raw algod errors into meaningful ones       |

The client is configured through setter methods — `set_signer()` and `set_default_signer()`, and `set_default_validity_window()` and the suggested-params cache methods. Each returns the client, so configuration chains into one expression: `AlgorandClient.testnet().set_default_validity_window(50)`.

## Send a Transaction

Sending follows one pattern regardless of transaction type: fill in a params dataclass (`PaymentParams`, `AssetTransferParams`, `AppCallParams`, and so on) and pass it to the matching method on `algorand.send`. The client builds the transaction, signs it, submits it, and waits for confirmation:

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-py/refs/heads/main/examples/concepts/algorand_client.py"
  snippet="SEND_PAYMENT"
  lang="py"
  frame="none"
/>

The same pattern applies to every transaction type.

<LinkCard
  title="Transactions"
  href="/algokit-utils-py/concepts/core/transaction/"
  description="The params dataclasses, their common fields, and the structure of returned results"
/>

## Configure Signers Once

In the example above, the signer was passed explicitly. This is rarely necessary: the client maintains a registry of signers keyed by sender address, and any transaction without an explicit signer is signed by the signer registered for its sender. Accounts created or loaded through `algorand.account` (such as `algorand.account.random()` or `algorand.account.from_environment()`) register their signer automatically. For accounts obtained elsewhere, register the signer explicitly:

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-py/refs/heads/main/examples/concepts/algorand_client.py"
  snippet="SIGNER_CONFIG"
  lang="py"
  frame="none"
/>

Signers are resolved in order: an explicit `signer` on the params wins, then the signer registered for the sender's address, then the default signer if one was set.

<LinkCard
  title="Accounts"
  href="/algokit-utils-py/concepts/core/account/"
  description="The account types you can register and the ways to create them"
/>

## Use the SDK Clients Directly

AlgoKit Utils wraps [`py-algorand-sdk`](https://github.com/algorand/py-algorand-sdk) rather than replacing it. For operations the library doesn't cover, the underlying SDK clients remain directly accessible:

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-py/refs/heads/main/examples/concepts/algorand_client.py"
  snippet="SDK_CLIENTS"
  lang="py"
  frame="none"
/>

Which clients are available depends on how the `AlgorandClient` was created: `default_localnet()` configures all three, `testnet()` and `mainnet()` configure algod and indexer but not the Key Management Daemon (KMD), and `from_environment()` configures whatever the environment variables describe. Accessing a client that was not configured raises an error identifying the missing configuration.

## Tune Network Defaults and Caching

Every transaction the client builds consumes _suggested parameters_ — algod's recommended values for the fee, the current round (which anchors the transaction's validity range), and network identifiers such as the genesis hash. Rather than paying a round trip to the node for every transaction, the client caches the last response briefly. Both the caching behavior and the defaults derived from it are adjustable:

<RemoteCode
  src="https://raw.githubusercontent.com/algorandfoundation/algokit-utils-py/refs/heads/main/examples/concepts/algorand_client.py"
  snippet="SUGGESTED_PARAMS_CONFIG"
  lang="py"
  frame="none"
/>

Each call adjusts one behavior:

- `set_default_validity_window()` sets how many rounds each transaction remains valid, from its first-valid to its last-valid round.
- `get_suggested_params()` returns the cached parameters, fetching fresh ones from algod only when the cache has expired.
- `set_suggested_params_cache()` seeds the cache directly — useful when you have already fetched parameters or want to pin modified values.
- `set_suggested_params_cache_timeout()` controls how long a fetched response is reused; a timeout of `0` disables caching, so every transaction fetches fresh parameters.

The default validity window is 10 rounds (roughly 30 seconds), except on LocalNet, where it is 1000 rounds so that transactions don't expire during debugging. Individual transactions can override the client-wide default through the `validity_window`, `first_valid_round`, and `last_valid_round` fields present on every params dataclass. A small window is usually the right choice: it bounds the period during which a transaction you have written off as failed can still be validly submitted.

## What's Next

<CardGrid>
  <LinkCard
    title="Accounts"
    href="/algokit-utils-py/concepts/core/account/"
    description="Creating, loading, and funding accounts, and signers in detail"
  />
  <LinkCard
    title="Transactions"
    href="/algokit-utils-py/concepts/core/transaction/"
    description="Payments, atomic groups, simulation, resource population, and fees"
  />
  <LinkCard
    title="Applications"
    href="/algokit-utils-py/concepts/building/app-client/"
    description="Deploying and calling smart contracts"
  />
  <LinkCard
    title="AlgorandClient API Reference"
    href="/algokit-utils-py/api/algokit_utils/algorand/"
    description="Every method and property on the client"
  />
</CardGrid>
